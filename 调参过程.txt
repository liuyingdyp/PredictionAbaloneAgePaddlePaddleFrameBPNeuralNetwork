1.经过每次运行代码，发现十个测试集中存在一个鲍鱼年龄的预测值与真实值相差较大，
之后将random_state=1改成0，让每次随机取不同的数据集进行试验。
2.一共六层神经网络，经过不断地进行训练—测试，来调整中间隐藏层的神经元（neurons）
个数，最后确定了各层的neurons个数。输入层8个neurons，中间分别是200,800,800,
100个neurons，最后输出层1个neuron。
3.防止过拟合在神经网络层中间设置dropout函数，让每层的50%的神经元进行训练。效果会更好些。
4.数据批次batch_size=16改成batch_size=20。
